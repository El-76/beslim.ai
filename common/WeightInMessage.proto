syntax = "proto3";

package beslim.ai;

message Coords {
    int32 vx = 1;
    int32 vy = 2;
    float x = 3;
    float y = 4;
    float z = 5;
}

message Snapshot {
    bytes photo = 1;
    repeated Coords grid = 2;
}

message WeightInMessage {
    repeated Snapshot snapshots = 1;
}

